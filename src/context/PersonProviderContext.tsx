import React from 'react';

/**
 * This is a blank, shareable context that the App will load a PersonProvider into a launch.
 */
export const PersonContext = React.createContext<any | undefined>(undefined);
