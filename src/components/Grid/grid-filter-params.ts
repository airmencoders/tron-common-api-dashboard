/**
 * Interface for Grid Filter Params
 * @interface 
 * @member filterOptions list of Filter Types to show (for example: ['contains', 'notContains'])
 */
export interface GridFilterParams {
  filterOptions?: string[];
}